import { Routes } from '@angular/router';
import { ServicesTestComponent } from './services-test/services-test.component';

export const routes: Routes = [
  { path: 'test-services', component: ServicesTestComponent },
 
];